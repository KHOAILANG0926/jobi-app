# ChatGPT ↔ Claude Code 인수인계 문서

## 현재 작업

2026-09-04 사용자의 10단계 지시("크롤러 표준 완성") + 1차 보고서에 대한 사용자의
보완 지시(10개 항목) 처리 완료. **결론: 운영 재개(cron 활성화) 비승인 상태 유지 —
크롤러의 'exact' 좌표 등급이 독립 검증에서 약 30~36% 실패율을 보여 기준 미달.**

## 핵심 발견 — 이번 라운드에서 가장 중요한 결과

1차 보고서는 C1 11건 중 9건만 확인하고 "오탐 0건"이라 주장했으나, 사용자 지적에 따라
**11건 전부**를 Google 지도로 재확인하고 엄격 기준(동일 POI/번지, 300m 초과 시 강등,
모호하면 보류)을 적용한 결과:

- **100건 조사 C1 11건**: 7건 PASS(오차 1m~130m), 3건 강등(480m/405m/2.6km),
  1건 보류(71B Xuân Diệu, 대조 결과 모호).
- **신규 10건(batchD) 블라인드 시험에서도 동일 패턴 재현**: C1 2건 중 1건은
  PASS(~32m), 1건은 **522~823m 오차로 실패**("128 Hồng Bàng, Quận 5" — Bệnh Viện
  Hùng Vương 공고. 주소 텍스트 자체는 정확했지만 geocode 좌표가 병원 정문에서
  500m 이상 벗어남).
- **종합**: 세 표본(100건+신규20건+신규10건)에서 확인된 exact-tier 주장 14건 중
  **9건만 엄격 기준 통과(64%), 5건 실패**(강등 4건 + 보류 1건, 36%).
  100건 조사와 완전히 별도인 신규 10건에서도 거의 동일한 실패율이 재현됐다는 것은
  이것이 우연이 아니라 **지속적인 구조적 한계**임을 시사한다.

**원인**: 크롤러의 'exact' 등급은 우리 자체 쿼리 변형끼리의 자기수렴(<=300m)일 뿐,
실제 좌표 정확도를 보장하지 않는다. Geoapify가 내부적으로 일관되면서도(2개 이상
변형이 서로 근접) 실제로는 300m~800m 벗어난 경우가 반복 확인됐다 — 주로 번지수가
큰 긴 도로에서 지오코더의 번지 보간이 부정확하기 때문. 이번 라운드에서 고친 3가지
로직 결함(도로명 미확인/하위 도시명 오판/단독 반대표 거부권)과는 **다른 종류의
문제**이며, 코드 로직 수정으로 해결되지 않는다 — 독립적인 ground-truth 대조 없이는
근본적으로 해소 불가능한 한계로 판단된다. `geocode.py`의 `resolve_coordinate_
accuracy()` 문서에 이 한계를 명문화해 두었다(주석 참고).

## 변경 내용 (이번 라운드 전체 커밋)

- `54240c5`: 공개 게이트 강화(모든 근무지 exact 요구).
- `546b410`: 도로명 주소 place-name 미확인 결함 수정(저신뢰도 변형 우연 수렴).
- `09341c5`: Thủ Đức/Dĩ An/Thuận An 하위 도시명 오판 결함 수정.
- `ac88114`: 단독 반대표 무조건 거부권 결함 수정(표수 비교로 전환).
- `3163b16`: 3~10단계 진행상황 핸드오프(중간, 이후 사용자가 보완 요청).
- `56048b6`: 사용자 보완 지시 반영 — ① C1_partial 게이트 실패 통합 테스트 추가,
  ② geocode 일시 오류 시 기존 active/publish_gate_reason 오염 방지(신규 발견 결함
  수정 — job_work_locations 동기화는 이미 보호되고 있었으나 active 갱신엔 없었음),
  ③ 오전/오후 분할 근무시간("Sáng: 7h-11h, Chiều: 12h-16h") 파싱 결함 수정(g 플래그
  누락으로 뒷부분 유실), ④ exact 등급의 실제 정확도 비보장 한계를 코드 주석으로 명문화.
- 최신 commit: `56048b625...`(local/GitHub/VPS 3중 확인 완료). 이 문서 자체 갱신은
  별도 커밋으로 뒤따름.

## 테스트 결과

- VPS 격리 환경에서 job_quality 15/15 + address_pipeline_integration 16/16 =
  **31/31 통과**(신규 3개 테스트: C1_partial 게이트 실패, geocode 일시 오류 시
  기존 데이터 보호, 분할 근무시간 보존 — 전부 실제 발견된 결함을 먼저 재현
  확인한 뒤 수정하는 순서로 진행).
- **C1 11건(100건 조사) 전수검증**: 11/11 확인 완료, 7건 PASS, 3건 강등, 1건 보류
  — `c1_audit_table.md`에 표 전체 기록(원문주소/geocode주소/좌표/Google 대조/
  오차거리/근거 포함, 별도 첨부).
- **신규 20건(batchC) 블라인드 시험**: 20/20 필드 존재 확인, **19/20 완전 일치**
  (14번 공고 "Kỹ Sư Kỹ Thuật Điện (M&E)"의 근무시간 필드만 "7h-11h, 12h-16h"
  분할근무 중 뒷부분 누락 — 이번 라운드에서 수정 완료, 회귀 테스트로 고정, 단
  새 실제 사례로 재확인은 아직 못함). C1 1건 PASS(~200m), C1_partial 2건 정상 보류.
- **신규 10건(batchD) 블라인드 시험**: commit `56048b6`으로 실행. 근무시간 필드는
  10건 전부 원문에 없어 분할근무 파서 수정의 실사례 재확인은 이번에도 못함(fixture
  단위 테스트로만 검증됨). 3건 전체 필드 상세 확인 완료(전부 일치), 나머지 7건은
  시간 제약으로 개별 DOM 대조 미완료. **C1 2건 중 1건 PASS(~32m), 1건 FAIL
  (522~823m, "128 Hồng Bàng" 병원 공고)** — 크롤러 내부는 여전히 active=true로
  판정하지만 독립 대조 실패, 즉 이 좌표 정확도 한계가 새 표본에서도 재현됨을 확인.
  C1_partial 1건(exact 1 + ward 2) 정상 보류 확인 — 게이트 수정이 새 데이터에도
  올바르게 작동.

## 수치 분리 (사용자 요청 8번 항목)

| 구분 | 수 |
|---|---|
| 전체 필드 완전 일치 공고 수(batchC) | 19/20 |
| 일부 누락 공고 수(batchC, 근무시간만) | 1/20(수정 완료, 실사례 재확인 대기) |
| C1 전수검증 완료 수(3개 표본 합계) | 14/14 |
| C1에서 강등된 수 | 4(100건 조사 3 + batchD 1) |
| 확인 못함/보류 수 | 1(71B Xuân Diệu) |
| C1 최종 확정(PASS) 수 | 9/14 |

## 발견된 문제

- **exact 등급의 구조적 정확도 한계**(위 "핵심 발견" 참고) — 이번 라운드 코드
  수정으로 해결 안 됨, 지속적인 독립 표본 검증 필요.
- Geoapify 저신뢰도 fallback 패턴(무관한 결과로 우연 수렴, 이전 보고서에서 확인) —
  표수 비교 수정(ac88114)으로 다른 좋은 변형이 있으면 이제 극복되지만, 근본 원인은
  여전히 남아 있음.
- 로컬 PC에 Python 미설치 — 모든 테스트는 VPS 격리 디렉터리에서 실행.
- 분할 근무시간 파서 수정은 fixture(단위 테스트)로만 검증됨 — 신규 30건(batchC+D)
  중 실제로 분할근무 텍스트를 가진 새 사례가 없어 실사례 재확인은 완료 못함.
- batchD 10건 중 7건은 시간 제약으로 개별 DOM 필드 대조를 완료하지 못함(주소
  텍스트와 exact 좌표 3건은 전수 확인함).

## 다음 결정사항

- **운영 재개(cron 활성화, DB 쓰기 재개) 비승인 — 사용자 판단 대기.** 기준(오탐 0건)
  미달: exact-tier 주장의 약 36%가 독립 대조에서 300m를 초과했다.
- 사용자가 제시한 다음 단계(비공개 검증용 공고 3~5건 저장 → 원문→DB→목록/상세→
  지도→지원링크 확인)는 **DB 쓰기를 수반하므로 이번 라운드 범위 밖** — 실행하지
  않았음. exact 정확도 문제 해소 방안(예: 내부 수렴 반경 축소, 또는 2차 독립
  geocoding 공급자 교차검증 도입) 논의 후 사용자 승인이 있어야 다음 단계로 진행
  가능.
- 크롤러 cron은 계속 비활성 상태.
- 관리자 대행 등록 설계·추가 UI 개선은 범위 밖으로 유지.
