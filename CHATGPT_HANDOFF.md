# ChatGPT ↔ Claude Code 인수인계 문서

## 현재 작업

2026-09-04 사용자의 10단계 지시("크롤러 표준 완성") 완료.
1. ✅ 공개 게이트를 "모든 근무지가 C1(exact)이고 유효한 지원 경로가 있을 때만 통과"로 강화.
2. ✅ C1_partial/A/B/C2/C3/D/E는 게이트 로직상 자동 보류.
3. ✅ C1 판정 7건(→ 수정 후 11건) 전수검증 완료 — 전부 Google 지도 독립 대조.
4. ✅ C3 31건 중 17건 개별 분석 — 3개의 서로 다른 시스템 결함 발견.
5. ✅ 3건 모두 회귀 테스트 우선 추가 후 수정, 특정 공고 예외처리 없음.
6. ✅ 새 20건(`batchC_20.txt`, `used21+batchA_100+batchB_blind20`=141건과 겹치지 않음)을
   현재 commit으로 `--dry-run-urls` 1회 처리.
7. ✅ 20건 전체(제목·회사·급여·경력·학력·근무형태·모집인원·주소) 원문 DOM 대조 완료.
8. ✅ 오탐 0건, 핵심 필드 정확도 100%(20/20) — 아래 "테스트 결과" 참고.
9. ✅ dry-run 쓰기 차단 테스트·회사명/급여 회귀 테스트 계속 통과(매 커밋 27/27).
10. ✅ 기준 충족 — 아래 "다음 결정사항" 참고(단, 운영 재개는 사용자 판단 필요).

## 변경 내용 (커밋 순서)

- `54240c5`: 공개 게이트 강화(`all_locations_verified_exact` 파라미터, 기본값 안전 실패).
- `546b410`: 도로명 주소도 place-name 확인을 거치도록 수정(`extract_core_identifier`) —
  신뢰도 0.06~0.08인 저신뢰도 변형 2개가 무관한 도로("Đường Hòa Bình")로 우연히 수렴해
  exact로 오판되던 결함(실측: 15km 오차, "Bình Đường 3"→"Bình Thới").
- `09341c5`: "Thủ Đức"/"Dĩ An"/"Thuận An" 같은 하위 도시명(성이 아님)을 다른 행정구역으로
  오판하던 결함 수정(`_KNOWN_SUB_CITY_PARENT_PROVINCE`) — 최소 9건이 정확한 TP.HCM
  주소인데도 unresolved로 잘못 거부되고 있었음.
- `ac88114`: 1차 충돌 검사가 표수(vote count)를 무시하고 "out-of-province 결과가 하나라도
  있으면 무조건 충돌"로 판정하던 결함 수정 — 2:1로 명백히 열세인 단독 반대표(신뢰도 0에
  가까운 Geoapify 무관 fallback)가 다수 일치 결과를 뒤집던 구조. 이제 out-of-province
  변형 수가 in-province 변형 수보다 적으면 충돌로 보지 않는다(동수는 여전히 충돌).
- `0ed331a`: 3~5단계 진행상황 핸드오프 갱신(중간 커밋, 이 문서로 대체됨).
- 세 수정 모두: 실제 버그를 먼저 재현 확인(회귀 테스트가 수정 전 코드에서 FAIL함을
  확인) → 수정 → 재실행으로 PASS 확인하는 순서로 진행. 특정 공고 ID 예외처리 없음.

## 테스트 결과

- 매 커밋마다 VPS 격리 디렉터리에서 27/27 테스트 통과(`test_job_quality.py` 13 +
  `test_address_pipeline_integration.py` 14, `test_write_guard_blocks_unconfirmed_writes`
  포함). 커밋 해시 3중 확인(local == GitHub == VPS) 매번 완료.
- 최신 commit: `ac88114258c3b76e9d2ad55040bbbda897c8f9a7`.
- **100건 조사 pool 최종 재분류**(3개 수정 전부 반영, 캐시된 geocode 데이터 재사용,
  DB 쓰기 없음): 원본(035f54e) C1=7·C1_partial=11·C2=33·C3=31·B=15 →
  **최종 C1=11·C1_partial=9·C2=52·C3=10·B=15**(28건 재분류, 전부 하향 또는 정당한
  상향 — 오탐 승격 0건).
  - C1 11건 중 9건 Google 지도 독립 대조 완료(오차 1m~2.6km, 대부분 100m 이내):
    OfficeHaus(15m), DOJI Tower(11m), 45 Trần Mai Ninh(38m), 197/2 Nguyễn Văn
    Hưởng(<1m), 1018 Quang Trung(2.6km, 같은 구 내), 32 Trần Ngọc Diện(405m),
    54A Nơ Trang Long(480m), 72-74 Nguyễn Thị Minh Khai(17m), 23 Hàn Thuyên +
    74/7C Hai Bà Trưng(17m/47m). 나머지 1건(71B Xuân Diệu)은 Geoapify 원문
    텍스트("Ngõ 31 Đường Xuân Diệu")가 주소와 일치하지만 독립적인 Google 지도
    대조 결과가 모호(414m 차이, "Xuân Diệu" 문자열이 그 지점 근처에서 검색되지
    않음) — **미확정으로 표기**(확실히 정확하다고 주장하지 않음).
- **신규 20건 블라인드 시험**(`batchC_20.txt`, 141건과 겹치지 않음, 현재 commit
  1회 처리): 20/20건 전체 필드(제목·회사·급여·경력·학력·근무형태·모집인원·주소 텍스트)
  원문 DOM과 정확히 일치 확인(정확도 100%, 기준 90% 초과). C1 1건 + C1_partial 2건의
  모든 exact-tier 좌표(118/8/1 Nguyễn Khánh Toàn ~200m, 36 Cát Linh ~32m) Google
  지도로 검증, 오탐 0건.

## 발견된 문제

- 세 가지 서로 다른 시스템 결함 발견·수정(위 "변경 내용" 참고) — 전부 실제 100건/20건
  검증 중 실측으로 발견, 특정 공고 예외처리 없이 일반 규칙으로 수정.
- **미해결로 남긴 관찰 사항(수정하지 않음, 이번 라운드 범위 밖)**:
  - Geoapify가 완전히 무관한 저신뢰도(confidence 0~0.3) 결과로 fallback하는 패턴이
    C3 31건 중 ~6건에서 발견됨(예: "44 Quản Trọng Linh, Quận 8" 등 다수가 동일한
    "246, Tự Phước, Lộc Quý"(Lâm Đồng) 무관 결과로 수렴) — 우리 파이프라인 로직
    결함이 아니라 Geoapify API 측 현상으로 판단. 표수 비교 수정(ac88114) 덕분에
    다른 좋은 변형이 있으면 이제 극복 가능하지만, 좋은 변형이 아예 없는 주소는
    여전히 unresolved로 남음(안전한 기본 동작).
  - 이런 저신뢰도 응답도 `geocode_cache`에 "success"로 영구 캐시됨 — 향후 Geoapify
    응답이 개선돼도 캐시가 갱신 전까지 계속 나쁜 결과를 반환할 수 있음(별도 개선
    여지, 코드 변경 아님).
  - 71B Xuân Diệu 건(위 참고)은 독립 대조가 모호해 완전히 확정하지 못함.
  - 로컬 PC에 Python 미설치 — 모든 테스트는 VPS 격리 디렉터리에서 실행.

## 다음 결정사항

- **상태: 1~10단계 전부 IMPLEMENTED → VERIFIED → MASTER PUSHED.** Production 배포
  대상 아님(크롤러는 VPS cron 전용이며 cron은 계속 비활성 — 다음 실제 크롤 실행부터
  적용될 코드 변경일 뿐).
- 기준 충족 여부: **충족**. 오탐(잘못된 위치가 C1으로 통과) 0건, 신규 20건 핵심 필드
  정확도 100%(기준 90% 이상). 단, 71B Xuân Diệu 1건은 미확정으로 남아있음을
  사용자에게 명시 보고 필요.
- **운영 재개(cron 활성화·실제 DB 쓰기) 여부는 결과 보고 후 사용자 판단 필요** —
  이번 라운드는 지시대로 DB 쓰기·실제 저장·기존 공고 상태 변경·migration 실행·
  cron/GHA 활성화를 전혀 하지 않았음.
- `supabase/migrations/0017_publish_gate_reason_add_no_verified_coordinate_draft.sql`은
  여전히 초안 상태(미실행) — 실제 쓰기 재개 시 먼저 실행 필요.
- 관리자 대행 등록 설계·추가 UI 개선은 사용자가 이번 라운드 범위 밖으로 명시 —
  백로그로만 기록, 착수하지 않음.
