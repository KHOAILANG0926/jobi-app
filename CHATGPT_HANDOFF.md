# ChatGPT ↔ Claude Code 인수인계 문서

## 현재 작업

기존 국내 구인구직 흐름을 유지하면서 홈 첫 화면을 한국 취업 연결 차별점이 드러나는 하나의 브랜드 경험으로 개편했다.

## 변경 내용

- 홈 상단에 국내 일자리와 한국 취업 연결 메시지를 균형 있게 담은 2열 히어로를 추가했다.
- 기존 검색 상태와 결과 목록을 재사용한 검색창을 히어로 안에 배치하고 검색 시 결과 영역으로 이동하도록 연결했다.
- 별도 이미지 의존성 없이 서울 타워·스카이라인을 표현하는 CSS 비주얼을 추가했다.
- 기존 한국 상담 배너, 스킬업 광고, 로그인/프로필 안내를 `Khám phá cơ hội dành cho bạn` 아래 동일한 카드 규칙으로 재배치했다.
- 브랜드·지역·카테고리·추천 필터와 한국 상담 모달, 기존 라우트 및 배너 CTA는 변경하지 않았다.
- 태블릿에서는 2열 카드, 모바일에서는 1열 히어로·카드와 전체 폭 검색으로 전환되도록 반응형 규칙을 추가했다.

## 테스트 결과

- 홈 구조 회귀 검사: 통과
- 데스크톱(1440px) 히어로 여백·검색·상담 모달·브랜드/지역 링크 검사: 통과
- 모바일(390px) 검색 노출·3개 카드 유지·가로 넘침 검사: 통과
- 주요 라우트(`/`, `/viec-han-quoc`, `/ho-so`, `/cong-dong`, `/tinh-luong`) 스모크 검사: 통과
- `npx tsc --noEmit`: 통과
- `npm run build`: 통과

## 발견된 문제

- 일부 기존 브랜드 파비콘 외부 URL이 404를 반환하지만 홈 개편과 무관하며 대체 로고 fallback이 동작한다.
- `0003_interviews.sql`은 아직 운영 DB에 적용되지 않아 실제 E2E가 보류 상태다.
- 지정된 미추적 HTML 파일은 건드리지 않았다.

## 다음 결정사항

1. 원격 DB 작업이 다시 허용되는 시점에 최신 `0003_interviews.sql`을 적용하고 interviews 실제 E2E를 검증한다.
2. 그 전까지는 기존 채용 흐름과 홈의 접근성·오류 처리 회귀를 원격 DB 없이 점검한다.
